import 'package:flutter/material.dart';

class ThemeColors {
  static const Color primary = Color(0xFF002C53);      // Your main color
  static const Color cardBackground = Color(0xFFFFFFFF); // White for cards
  static const Color background = Color(0xFFC7C7C7);   // Background gray
  static const Color text = Color(0xFF000000);         // Black text

}
